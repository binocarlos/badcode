Hero plate images for the homepage Atlas nodes.
Drop 16:9 images here named to match the node's plate path in src/home/timeline.ts:
  camping.webp   -> the Camping node
  karen.webp     -> the Karen node
Until a file exists the node shows a dim backing panel (no error).
