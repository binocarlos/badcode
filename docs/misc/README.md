# misc/ — everything that fits nowhere else

The catch-all for anything that isn't a story (`../stories/`), an idea seed
(`../ideas/`), or a marketing plan (`../marketing/`). Better parked here than lost —
if a theme accumulates, promote it to its own section.

Current residents:

- [`home-graph.md`](./home-graph.md) — homepage graph/structure notes
- [`AGENTS_RESEARCH.md`](./AGENTS_RESEARCH.md) — raw research substrate behind the agent-orange design spec
